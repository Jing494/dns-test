# DNS 测试方法指南

> **适用对象**: AI助手 / 网络测试人员  
> **最后更新**: 2026-08-09  
> 📍 目录可自由放置（脚本相对定位），本文示例路径 `/workspace/dns-test` 为当前环境默认，路径不同请替换。

---

## 一、测试目标

验证 DNS 服务器的可用性、稳定性、解析准确性，以及特殊场景（如 VoWiFi/IMS）的支持能力。

---

## 二、测试环境要求

| 工具 | 用途 | 状态 |
|------|------|------|
| `dig` | DNS 记录查询 | ✅ 必需 |
| `ping` | ICMP 连通性测试 | ✅ 可选（无权限时该项跳过） |
| `bash` | 批量测试脚本 | ✅ 必需 |
| `perl` | 专项测试脚本 | ✅ 专项测试需要 |

> 说明：测试脚本已全部使用 `sed` 兼容写法，Linux/macOS 均可直接运行。

---

## 三、评分标准

| 等级 | 综合评分 | 说明 |
|------|---------|------|
| ⭐⭐⭐⭐⭐ | 95-100% | 优秀，推荐使用 |
| ⭐⭐⭐⭐ | 80-94% | 良好，可用 |
| ⭐⭐⭐ | 60-79% | 一般，存在问题 |
| ⭐⭐ | <60% | 差，不建议使用 |

### 评分口径（total_all / success_all 累加规则）

- 综合评分 = `success_all / total_all × 100%`（四舍五入取整）。
- 每个测试点（单个域名 / 单轮稳定性 / 单个记录类型等）通过才累加 `success_all`，测试点一旦发起即累加 `total_all`（不可达预检跳过、信息项除外）。
- 第 8 项「一致性」：统计 3 个域名的 A 记录覆盖率，**按实际结果计数**——至少 1 个域名成功解析才计该项通过（不恒计成功）；全部失败则该项计入 `total_all` 但不计 `success_all`。
- 第 14 项「结果对比」：以"判定数"计入 `total_all`（对比基准不可达的域名不计入），本地与基准结果一致、或差异为 CDN/负载均衡域名时计入 `success_all`。该项为**与阿里 DNS 的解析结果对比，仅作疑似劫持提示**，非绝对劫持判定。
- 信息项（3GPP/VoWiFi 等）不参与 `total_all` / `success_all`。

---

## 四、测试维度

### 完整版测试（16 项，评分项共 78 个测试点；IPv6 环境不可用时 7b 项跳过，为 77 点）

| 编号 | 维度 | 测试内容 | 通过标准 |
|------|------|---------|---------|
| 1 | A 记录 | 25 个域名（15国内+10国际） | 100% 成功 |
| 2 | AAAA 记录 | 8 个域名 | 100% 有记录 |
| 3 | 3GPP/VoWiFi | 3 个域名（epdg.mnc011/mnc000/mnc001 代表，运营商ePDG全量检测见专项carrier_epdg.pl） | **信息项，不计入评分** |
| 4 | 记录类型 | MX/NS/TXT/CNAME/SOA | 全部支持 |
| 5 | 稳定性 | 20 次连续查询（可用环境变量 `STAB_ROUNDS` 调整） | 100% 成功 |
| 6 | 异常测试 | NXDOMAIN | 正确返回 |
| 7 | 连通性 | Ping 解析出的 IP | 可连通 |
| 7b | **IPv6连通性** | ping6 实际连通（无IPv6环境自动跳过） | 可连通 |
| 8 | 一致性 | A/AAAA 覆盖率 | 100% |
| 9 | 运营商域名 | 10086/chinaunicom/189/10000 | 100% 成功 |
| 10 | DNSSEC | DNSKEY/DS/RRSIG | 支持 |
| 11 | ECS | EDNS Client Subnet | 支持 |
| 12 | PTR | 反向 DNS 解析 | - |
| 13 | TTL 分析 | TTL 值分布 | 合理范围 |
| 14 | 结果对比（疑似劫持提示） | 与阿里 DNS 对比 | 一致（CDN/负载均衡差异除外） |
| 15 | 递归查询 | ra 标志 | 支持递归 |

### 精简版测试（10 项，评分项共 64 个测试点；IPv6 环境不可用时 7b 项跳过，为 63 点）

| 编号 | 维度 | 测试内容 |
|------|------|---------|
| 1 | A 记录 | 25 个域名 |
| 2 | AAAA 记录 | 8 个域名 |
| 3 | 3GPP/VoWiFi | 3 个域名（信息项，不计入评分） |
| 4 | 记录类型 | MX/NS/TXT/CNAME/SOA |
| 5 | 稳定性 | 20 次连续查询 |
| 6 | 异常测试 | NXDOMAIN |
| 7 | 连通性 | Ping（IPv4） |
| 7b | **IPv6连通性** | ping6 实际连通（无IPv6环境自动跳过不计分） |
| 8 | 一致性 | A/AAAA 覆盖率 |
| 9 | 运营商域名 | 4 个运营商 |

---

## 五、扣分但正常的情况说明

以下情况会导致测试扣分，但属于**正常现象**，不影响 DNS 实际使用：

| 测试项 | 扣分原因 | 正常原因 | 影响 |
|--------|---------|---------|------|
| **PTR 解析** | 国内 IP 无 PTR 记录 | 很多企业/网站 IP 不配置反向解析 | ❌ 无影响 |
| **劫持检测** | CDN 域名结果不一致 | CDN 多节点负载均衡，不同 DNS 返回不同边缘节点 | ❌ 无影响 |
| **DNSSEC** | 部分域名无 DNSSEC | 不是所有域名都部署了 DNSSEC | ⚠️ 轻微，安全性略低 |
| **ECS** | 返回结果可能相同 | 单节点 CDN 或 ECS 未生效 | ⚠️ 轻微，CDN 调度可能不准 |
| **TTL** | TTL 值较短 | 短 TTL 便于快速切换，长 TTL 减少查询 | ❌ 无影响 |

### 3GPP/VoWiFi 信息项说明（2026-08-09 起）

VoWiFi 域名改为**信息项，不参与综合评分**：

- 解析出 `127.0.0.1`：运营商未部署 ePDG，属正常现象
- 公共 DNS 查不到记录：公共 DNS 不缓存运营商内部记录，属正常现象
- 仅当 dig 报错/无响应时才会显示失败提示（同样不影响评分）

---

## 六、实测结果

### 2026-08-09 实测（完整版，[7b]前 77 项口径）

| DNS 地址 | 类型 | 综合评分 | 平均延迟 | 稳定性 | 劫持检测 | 备注 |
|---------|------|---------|---------|--------|---------|------|
| `223.5.5.5` | IPv4 阿里 | **96%** (74/77) | 37ms | 95% | ✅ 3/3 | 劫持误报修复后复测 |
| `114.114.114.114` | IPv4 114DNS | 94% (73/77) | 62ms | 100% | ✅ 3/3 | 劫持修复前数据，复测预计更高 |

### 2026-08-09 云南电信 DNS 可用性验证

4 个云南电信 DNS 全部可达，v4/v6 均正常解析（www.baidu.com / www.qq.com / epdg.mnc011）。

### 2026-08-09 云南电信 lite 版实测（[7b]前，63 项口径）

| DNS 地址 | 类型 | 综合评分 | Ping延迟 | 稳定性 |
|---------|------|---------|---------|--------|
| `240e:52:4800::8888` | IPv6 | **100%** (63/63) | 52.6ms | 100% |
| `240e:52:4000::8888` | IPv6 | **100%** (63/63) | 48.9ms | 100% |
| `222.172.200.68` | IPv4 | **100%** (63/63) | 42.0ms | 100% |
| `61.166.150.123` | IPv4 | **100%** (63/63) | - | 100% |

> 注：完整版历史数据见下方 2026-08-08 记录（修复前 79 项）；修复后 3GPP 改信息项、[7b] 加 IPv6 连通项，当前评分项为 78/64。

### 云南电信完整版正式实测（2026-08-09，78 项，含 IPv6 连通性）

| DNS 地址 | 类型 | 综合评分 | 平均延迟 | 稳定性 |
|---------|------|---------|---------|--------|
| `240e:52:4800::8888` | IPv6 | **96%** (75/78) | 18ms | 100% |
| `240e:52:4000::8888` | IPv6 | **97%** (76/78) | 19ms | 100% |
| `222.172.200.68` | IPv4 | **97%** (76/78) | 15ms | 100% |
| `61.166.150.123` | IPv4 | **97%** (76/78) | 20ms | 100% |

> [7b] IPv6实际连通性实测：百度 `240e:ff:e020:99b:0:ff:b099:cff1` 平均 45.6ms，本机 IPv6 通路正常。
> 扣分项均为正常现象：个别国际/运营商域名偶发解析波动（如 10086.cn 重试 3/3 全通）、PTR 无记录、TTL 波动等，不影响实际使用。

### 路由器转发测试（2026-08-09）

| 路由器 | 结论 |
|--------|------|
| `192.168.1.1` | ✅ 5/5 域名与云南电信省级DNS一致 → **确认转发到省级DNS** |
| `192.168.2.1` | ✅ 5/5 域名与云南电信省级DNS一致 → **确认转发到省级DNS** |

> 测试域名：epdg.epc.mnc011 / www.baidu.com / www.qq.com / www.taobao.com

### 2026-08-08 历史记录（修复前）

#### 完整版测试结果（v4，78 项）

| DNS 地址 | 类型 | 综合评分 | 延迟 | 稳定性 | DNSSEC | ECS | PTR | 劫持检测 | 递归 |
|---------|------|---------|------|--------|--------|-----|-----|---------|------|
| `240e:52:4800::8888` | IPv6 | 96% (75/78) | 25ms | 100% | ✅ 3/3 | ✅ | ⚠️ 1/3 | ✅ 2/3 | ✅ |
| `240e:52:4000::8888` | IPv6 | 97% (76/78) | 21ms | 100% | ✅ 3/3 | ✅ | ⚠️ 1/3 | ✅ 3/3 | ✅ |
| `222.172.200.68` | IPv4 | 96% (75/78) | 23ms | 100% | ✅ 3/3 | ✅ | ⚠️ 1/3 | ⚠️ 2/3 | ✅ |
| `61.166.150.123` | IPv4 | 96% (75/78) | 27ms | 100% | ✅ 3/3 | ✅ | ⚠️ 1/3 | ✅ 3/3 | ✅ |

#### 精简版测试结果（v3，66 项）

| DNS 地址 | 类型 | 综合评分 | 延迟 | 稳定性 |
|---------|------|---------|------|--------|
| `240e:52:4800::8888` | IPv6 | **100%** (66/66) | 22ms | 100% |
| `240e:52:4000::8888` | IPv6 | **100%** (66/66) | 22ms | 100% |
| `222.172.200.68` | IPv4 | **100%** (66/66) | 22ms | 100% |
| `61.166.150.123` | IPv4 | **100%** (66/66) | 22ms | 100% |

> 注：修复后 3GPP 改信息项，完整版评分项由 79 → 77，精简版由 66 → 65，历史数据不可直接对比。

---

## 七、脚本使用

### 🤖 AI 助手引导（重要）

AI 使用本工具集时，请遵循 [AI_GUIDE.md](./AI_GUIDE.md) 的完整操作手册。核心流程：

1. **先问用户测哪个 DNS**（云南电信/阿里/腾讯/自定义），不要自作主张
2. **再问版本**：精简版(lite) / 完整版(full)
3. **用户要求"专业/深入"** → 引导专项测试（VoWiFi/路由器转发/端口/DNS64/反向解析）
4. **有交互提问工具**（如 ask_user / user_interaction 等，命名因 agent 而异）：直接弹选项问；**无交互提问工具**（纯文本/API）：引导用户跑 `bash dns-test.sh` 或用索引参数直接执行
5. **防超时铁律**：full 多 DNS 必须加索引参数（`bash full.sh A B 0`）

### 引导使用

```bash
# 交互式引导（推荐，可先选DNS组：1云南电信/2阿里/3腾讯/4全部）
bash dns-test.sh

# 非交互（自动精简版+单DNS，防超时）
bash dns-test.sh 8.8.8.8 </dev/null

# 预设快捷测试（云南电信/阿里/腾讯）
bash dns-preset.sh ali lite 0
```

### 对照DNS（阿里/腾讯，官方地址）

| 厂商 | IPv4 | IPv6 |
|------|------|------|
| 阿里云 | 223.5.5.5 / 223.6.6.6 | 2400:3200::1 / 2400:3200:baba::1 |
| 腾讯DNSPod | 119.29.29.29 | 2402:4e00:: / 2402:4e00:1:: |

### 效率机制

- **环境自检标注**：输出头部 `🌐 环境: ... | IPv6:可用/不可用`，结果可回溯（沙箱 vs 真机）
- **可达性预检**：测试前 dig 探测（2秒超时），不可达 DNS 直接跳过，避免无效白等
- **A记录并行**：25 个域名 8 并发查询，单次 dig 同时提取 IP 和延迟
- **快速模式**：`STAB_ROUNDS=5 bash lite.sh 8.8.8.8`（稳定性轮次从 20 降到 5）
- **自动化回归**：`bash smoke_test.sh` 一键验证核心功能（改动后必跑）
- **域名外置**：`CONFIG_DOMAINS=配置 bash lite.sh` 可覆盖测试域名列表（格式：`DOMAINS_MAIN=("a.com" "b.com")`）。⚠️ **安全警告**：配置文件会被 `source` 执行，**只加载你自己生成的配置文件**；脚本已做基础校验（含 `$`/`;`/eval/rm 等特征会拒绝加载），但来源不明的文件仍不要使用
- **日志保存**：`SAVE_LOG=1 bash lite.sh 8.8.8.8 0` 自动存 `results/lite-YYYYMMDD-HHMMSS.log`（Linux tee 同步终端+文件 / macOS 写文件；也可手动 `| tee results/x.log`）
- **DoH/DoT**：`bash tools/network/doh_dot_check.sh [DNS1,DNS2]`——**环境自适应**：DoT 用 `dig +tls=dot` 实测；DoH 有 curl 则 `curl --doh-url` 实测，无 curl 回退 443 端口级探测（实测结果受网络环境影响）。`bash install.sh` 校验时会检测 dig 是否支持 `+tls`（bind 9.18+），不支持则提示"DoT 检测降级为端口级"

### 直接运行

```bash
# 精简版（默认云南电信4个DNS）
bash lite.sh

# 完整版（自定义DNS）
bash full.sh 8.8.8.8

# 多DNS只测第1个（避免超时）
bash full.sh 8.8.8.8 114.114.114.114 0

# 稳定性轮次自定义
STAB_ROUNDS=5 bash lite.sh 8.8.8.8

# 保存测试结果到 results/ 目录
bash full.sh 8.8.8.8 0 | tee dns-test/results/full-$(date +%Y%m%d).log
```

### 专项测试

```bash
# VoWiFi 全域名解析（v4/v6 均可）
perl tools/vowifi/01_resolve_vowifi.pl 8.8.8.8

# VoWiFi 多DNS交叉验证
perl tools/vowifi/02_vowifi_verify.pl 8.8.8.8 114.114.114.114

# 路由器DNS转发测试（对比省级DNS，支持--分隔自定义基准）
perl tools/vowifi/03_test_router_dns.pl 192.168.1.1
perl tools/vowifi/03_test_router_dns.pl 192.168.1.1 -- 223.5.5.5
# 说明: --前=路由器IP，--后=省级对比基准(逗号分隔)；默认云南电信；也可用 PROVINCE_DNS 环境变量

# 端口连通性测试
perl tools/network/01_port_test.pl 223.5.5.5 53 udp
```

### 自定义 DNS

所有入口脚本和专项脚本均支持**参数传入**，无需修改脚本内容：

- 基础测试：`bash full.sh <DNS1> [DNS2] ... [索引]`
- 专项测试：`perl tools/xxx.pl <DNS1> [DNS2] ...`

**环境变量自定义（自己用最方便，不用改源码）：**

| 环境变量 | 作用 | 示例 |
|---------|------|------|
| `DEFAULT_DNS_CSV` | 覆盖基础测试默认DNS组（逗号分隔） | `DEFAULT_DNS_CSV="219.141.136.10,219.141.140.10" bash lite.sh` |
| `DEFAULT_DNS_NAME_CSV` | 覆盖默认DNS组显示名（可选） | `DEFAULT_DNS_NAME_CSV="北京电信1,北京电信2" bash lite.sh` |
| `PROVINCE_DNS` | 覆盖路由器测试对比基准 | `PROVINCE_DNS="223.5.5.5" perl tools/vowifi/03_test_router_dns.pl 192.168.1.1` |
| `ECS_SUBNET` | 覆盖 ECS 测试的 subnet（默认云南电信IPv6前缀） | `ECS_SUBNET="223.5.5.0/24" bash full.sh 8.8.8.8 0` |
| `PRESET_DNS_CSV` | dns-preset 自定义预设组 | `PRESET_DNS_CSV="1.2.4.8,8.8.8.8" bash dns-preset.sh custom lite 0` |
| `DNS_SERVER` / `DNS_LIST` | examples 默认 DNS | `DNS_SERVER="8.8.8.8" perl examples/01_dns_query.pl` |

如需调整默认测试目标（域名列表、稳定性轮次等），编辑 `lib/core.sh` 中的公共变量即可。
